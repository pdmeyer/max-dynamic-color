{
  "name": "max-dynamic-color",
  "version": "1.0.0",
  "description": "DynamicColor helper for Max jsui / v8ui / jspainter; build output in dist/ (yarn build).",
  "license": "MIT",
  "main": "dist/dynamicColor.js",
  "types": "dist/dynamicColor.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "@types/maxmsp": "^1.0.12",
    "typescript": "~4.9.5"
  }
}
